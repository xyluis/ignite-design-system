{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npx turbo run dev --parallel",
    "build": "npx turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.6.1"
  }
}
